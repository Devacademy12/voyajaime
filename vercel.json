{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "npm config set registry https://registry.npmjs.org/ && npm ci --legacy-peer-deps",
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "NPM_CONFIG_REGISTRY": "https://registry.npmjs.org/"
    }
  },
  "outputDirectory": ".next"
}
